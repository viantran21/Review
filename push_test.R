#' This is a test
#' We will run some little functions
#' Then save this as push_test.R


myvector <- seq(from = 100, to = 0, by = -7)

myavg <- mean(myvector)

mysum <- sum(myvector)

mysd <- sd(myvector)
